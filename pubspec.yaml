name: elderlycare_app

description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.1
  mjpeg_stream: ^1.0.1
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.0
  table_calendar: ^3.1.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/Assets/Images/LoginBanner.png